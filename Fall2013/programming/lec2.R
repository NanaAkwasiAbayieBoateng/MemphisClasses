#remainder if 199 / by 13
119%%13

#how many times 13 goes into 119
119%/%13


#find log base e of 103
log(103)

#find the value of 6 choose 2/combination
choose(6,2)

#generate 9 uniform values between 0 and 1
runif(9)

#find sin of pi/2
sin(pi/2)

#raise e to negative infinity
exp(-Inf)

(0:3)^Inf

#check if a number is finite
is.finite(10)


#check if a number is infinite
is.infinite(10)

#creating vectors 
#using
(y<-10:16)

#using c()
(y<-c(10,11,12,13,14,15,16))

#using scan()
y<-scan()
10
11
12
13
14
15
16

#using  rep
(y<-rep(3,4))

#using seq

y<-seq(from=10,to=18,by=1)
y
y<-seq(from=10,to=18)
y

#lets make some vectors
#make vector numbers 5 to 20 that has 5 to 20
y<-seq(5,20)

#make vector x that has 0,7,8
x<-c(0,7,8)

#join vectors using a<-c(numbers 5 to 20,x)
a<-c(y,x)

#select the 3,6 and 12 elements of a[c(3,6,12)]

(a[c(3,6,12)])


#select everything in a except 3-11 elements a[-(3:11)]

(a[-(3:11)])
#try x[c(0,2)]
(x[c(0,2)])

#try x[c(-2,3)]
 x[c(-2,3)]
rep(seq(2,20,by=2),2)

rep(c(1,4),c(3,2))

rep(1:4,each=2,time=2)



